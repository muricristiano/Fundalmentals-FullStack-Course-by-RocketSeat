Já conhecemos o git diff da aula passada, agora temos o --staged, segue diferença abaixp:

# git diff
Compara Working Directory com Local Repository


# git diff --staged
Compara Stage Area com Local Repository
