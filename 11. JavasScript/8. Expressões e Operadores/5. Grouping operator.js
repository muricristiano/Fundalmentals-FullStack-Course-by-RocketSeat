// Grouping operator

/* 
    Ordem de cáculo

    0. ( ) Concatenando aumenta uma camada de ordem, mas mesmo dentro dessa camada, se tiver uma soma e uma multiplicação, a ordem natural prevalece, exige daí, uma nova concatenação sempre que estiver no mesmo nível

    1. Multiplicação / Divisão
    2. Soma / Subtração



*/


let total = (2 + 5) * 2
console.log(total);