// Manipulando conteúdo

//innerText
const element = document.querySelector('h1')

//Alterando texto através do .innerText (igual ao textContent, mas deve haver alguma diferença)
element.innerText = 'Changed Title'