// Alterando estilos
const element = document.querySelector('body')

/* A diferença é que ao escolher o estilo, não separamos por traço "-", e sim por CamelCase

em vez de:

    background-color | backgroundColor

*/
element.style.backgroundColor = 'red'